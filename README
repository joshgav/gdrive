CLI for Google Drive

1. Get Google API credentials from
<https://console.cloud.google.com/apis/credentials> and store in
`.run/secrets.json`.
1. Ensure the Drive API is enabled for these credentials, e.g. `gcloud services enable drive`.
1. Then try it:

  ```
  yarn install && yarn link

  gdrive list [<start-of-name>]
  gdrive download <fileId>
  gdrive upload <path>
  ```

More verbs to come.
